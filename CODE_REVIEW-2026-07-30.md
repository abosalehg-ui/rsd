# 🔍 تقرير مراجعة شاملة — مشروع رصد (Rsd) v1.5

> **التاريخ:** 2026-07-30 • **النطاق:** المستودع بالكامل (Backend + Frontend + الأمن + التغليف + CI/CD)
> **المنهجية:** قراءة كاملة لجميع ملفات الكود المصدري (~8,500 سطر)، فحص تاريخ Git، تتبّع مسارات البيانات من الجامعين حتى العرض، مطابقة كل ملاحظة بموضع محدد (file:line).
> **ملاحظة:** المستودع خضع لمراجعتين سابقتين (AUDIT.md بتاريخ 2026-07-20 + PR #30)، والحالة الراهنة أفضل بوضوح من التقييم السابق (5.5/10).

---

## 1. نظرة عامة سريعة

**ما يفعله المشروع:** رصد لوحة تحكم شخصية لمراقبة المصادر المفتوحة (OSINT) للأحداث العسكرية والأمنية في الشرق الأوسط لحظياً. يجمع البيانات من 6 مصادر (GDELT، NewsAPI، RSS، UCDP، ADS-B، Iran OSINT) ويعرضها على خريطة تفاعلية 2D/3D مع تصنيف ذكي، تنبيهات صوتية، ودعم ثنائي اللغة عربي/إنجليزي RTL.

**البنية العامة:** فصل نظيف بين طبقتين — باك إند FastAPI (collectors → processors → models → api + middleware) وواجهة React أحادية الصفحة تستطلع الـ API دورياً. البيانات تُخزَّن في SQLite (async) وتُجمع عبر مجدول APScheduler، وتُعرض عبر Leaflet (2D) وglobe.gl/Three.js (3D).

**التقنيات المستخدمة:**
| الطبقة | التقنيات |
|--------|----------|
| Backend | FastAPI 0.140 · SQLAlchemy 2.0 (async) · aiosqlite · APScheduler · httpx · feedparser · pydantic-settings |
| Frontend | React 18 · Vite 7 · Tailwind CSS 3 · react-i18next · Leaflet · globe.gl + Three.js · lucide-react |
| الجودة/DevOps | pytest · vitest · ruff · ESLint · GitHub Actions (CI + Pages) · Docker · PyInstaller + Inno Setup · Tauri |

---

## 2. المراجعة التقنية والهندسية (وزن 45%) — **6.5/10**

### 2.1 جودة البنية المعمارية وفصل المسؤوليات — **6.5/10**

الطبقات حقيقية ومحترمة غالباً: `processors/` نقية بلا اعتماديات داخلية، ولا توجد دوائر استيراد. استخراج `_serializers.py` و`processors/dates.py` من تكرار سابق قرار صائب وموثّق.

لكن هناك انتهاكات:
- **`main.py` وحدة إله (300 سطر)** تخلط: إعداد السجل + دورة الحياة + بناء التطبيق + توصيل middleware + 4 نقاط API + منطق صحة الجامعين + تخديم الملفات الثابتة. لا يوجد `create_app()` factory، والتطبيق يُبنى وقت الاستيراد مع `_settings = get_settings()` (`main.py:94`) فتضطر الاختبارات لبناء تطبيق ثانٍ يدوياً (`conftest.py:44-48`). **الحل:** استخرج `create_app(settings)`، وانقل `/api/collectors/status` و`/api/sources` إلى `api/system.py`.
- **منطق أعمال داخل معالجات المسارات:** خوارزمية تسجيل الدول (40 سطراً من رياضيات المجال) داخل معالج HTTP في `events.py:233-272`؛ لا يمكن اختبارها إلا عبر HTTP. **الحل:** انقلها إلى `processors/scoring.py`.
- **انتهاك طبقات:** `api/iran.py:9` يستورد `get_leaders_list` من `collectors/`، فتعتمد طبقة الـ API على `feedparser` و`httpx` لمجرد سرد 10 أسماء. **الحل:** انقل `IRANIAN_LEADERS` إلى `app/data/` أو `processors/`.
- **لا آلية migrations** (`database.py:151` يستخدم `create_all` فقط). أعمدة مثل `confidence`/`video_url` مُعلَّمة كإضافات لاحقة (`database.py:59-61`). لتطبيق سطح مكتب مشحون تعيش قاعدته في `%LOCALAPPDATA%`، سيحصل مستخدمو v1.4 المرقّون إلى v1.5 على `OperationalError: no such column`. **الحل:** أضِف Alembic أو خطوة `ALTER TABLE` في `init_db`.

### 2.2 قابلية القراءة والصيانة — **6.5/10**

`processors/` و`middleware/` تُقرأ ببراعة (دوال < 30 سطراً، غرض واحد). لكن:
- **دوال طويلة عميقة التداخل:** `_process_iran_feed` (`iran_osint.py:160-264`) = 105 أسطر بـ5 مستويات تداخل، و`_process_feed` (`rss_feeds.py:162`) يحمل `# noqa: C901` — اعتراف صريح بالتعقيد بدل إصلاحه.
- **أرقام سحرية:** صندوق ADS-B الجغرافي مثبّت في رابط (`adsb.py:16` — `lat/29/lon/42/dist/2000`) فمنطقة التغطية غير قابلة للضبط وغير موثّقة؛ `.limit(200)` مثبّت في `events.py:95`؛ عوامل تحويل الوحدات بلا أسماء (`adsb.py:198-199`).
- **تعليقات انحرفت عن الكود (مُضلِّلة):** `scheduler.py:47` يقول "كل 5 دقائق" بينما `config.py:62` يضبط 120 ثانية؛ `main.py:107-108` يصف ترتيب Starlette عكسياً (خطأ فعلي — انظر 2.4)؛ `database.py:143` مسار قاعدة بيانات افتراضي يناقض `config.py:32`.

### 2.3 التكرار (DRY) وCode Smells — **6.0/10**

ثلاث عمليات إزالة تكرار حقيقية نُفّذت وموثّقة (`text_analysis.py`، `dates.py`، `_serializers.py`). لكن أكبر تكرار متبقٍ:
- **`_process_feed` مقابل `_process_iran_feed`** (~60 سطراً، ~85% متطابقة). الاختلاف الوحيد: حد الإدخالات (15 مقابل 20)، الاقتطاع (600 مقابل 500)، واستراتيجية التصنيف. **الحل:** `collectors/_feed_base.py::process_feed(client, cfg, mapper, ...)` حيث `mapper` هو الكود الوحيد الخاص بكل مصدر — يزيل ~60 سطراً ويجعل الدالة قابلة للاختبار.
- **فاصل 1800 مثلّث** في `scheduler.py:85` و`main.py:205` و`main.py:263` (سلسلة عربية مثبّتة، فأي تغيير للجدولة يجعل `/api/sources` يكذب). **الحل:** `iran_osint_interval` في `Settings`.
- **مصفوفتان متوازيتان يدوياً** في `main.py:58-70` و`main.py:158-176` تُطابقان نتائج `gather()` بالعين — إعادة ترتيب واحدة تنسب النتائج للمصدر الخطأ بلا خطأ. **الحل:** `COLLECTORS` tuple على مستوى الوحدة.
- **كود ميت:** `ucdp.py:45-48` `page=0` بلا حلقة تصفّح؛ `config.py:48-49` إعدادات OpenSky غير مُشار إليها في أي مكان.

### 2.4 معالجة الأخطاء والحالات الحدّية — **6.0/10**

المهلات مثالية (كل استدعاء httpx له timeout صريح)، وتحليل التواريخ/JSON كامل ومُختبَر. لكن:
- **🔴 ترتيب middleware معكوس (باگ وظيفي):** `add_middleware` في Starlette يُدرِج في الفهرس 0 والمكدس يُبنى معكوساً، فآخر مُسجَّل = **الأبعد للخارج**. `CORSMiddleware` مُسجَّل أولاً (`main.py:95`) = الأعمق، فكل استجابة 429 من `RateLimitMiddleware` (`ratelimit.py:65-69`) **تخرج بلا ترويسة `Access-Control-Allow-Origin`**. المتصفح يحوّلها لخطأ شبكة معتم، والواجهة لا تقرأ `Retry-After` أبداً. **الحل:** سجّل `CORSMiddleware` أخيراً + صحّح التعليق + اختبار تراجع.
- **الجامعون يحوّلون كل فشل إلى 0:** النمط في `gdelt.py:94-98` وأشباهه يضع `except` خارج `return`، فيصبح فشل الشبكة/JSON/DB مساوياً لـ"لا أخبار جديدة" — لا يستطيع المشغّل تمييز جامع معطّل من هادئ، ما يبطل غرض `/api/collectors/status`. **الحل:** أعِد `CollectResult(count, error)` أو ارفع الاستثناء ليصله `gather(return_exceptions=True)`.
- **`except Exception: continue` صامت تماماً** في `adsb.py:85-86` — أي تغيير مخطط من adsb.lol يُسقط 100% من الرحلات بصمت.
- **باگ نوع فعلي:** السلسلة `"ground"` تُكتب في عمود `Float` (`adsb.py:194` → `FlightTrack(altitude=...)`)، فتعيد `/api/flights/live` قيمة `"altitude": "ground"` حيث تتوقع الواجهة رقماً.
- **باگ منطق مجال:** رموز الطوارئ 7500 (اختطاف)/7600/7700 تُصنَّف "عسكرية" (`adsb.py:35`)، فطائرة مدنية تعلن طوارئ تظهر في `/api/flights/military` — واختبار `test_adsb.py:43-45` **يحمي الخطأ**.
- **`None` غير معالَج من `get_session_factory()`** في كل المسارات الـ11 (مثل `events.py:26-27`) — طلب قبل `init_db` يعطي 500 بدل 503.
- **فقد falsy-vs-None للإحداثيات:** `ucdp.py:103-104` (`if lat`) يستبدل حدثاً على خط الاستواء (lat=0.0) بمركز الدولة؛ نفس النمط في `adsb.py:194,200`.
- **تجاوز rate-limit عبر X-Forwarded-For** (`ratelimit.py:37-41`) — التعليق يفترض وكيلاً موثوقاً لكن الكود لا يتحقق، فأي عميل يرسل رأساً عشوائياً لكل طلب ويحصل على إنتاجية غير محدودة.

### 2.5 الأداء واستهلاك الموارد — **7.0/10**

عمل تحسين سابق مثير للإعجاب وموثّق: إصلاح N+1 في `/api/iran/leaders`، تجميع SQL بدل التحميل للذاكرة، snapshot للطيران يمنع تضخم الطلبات، إزالة تكرار كتابة المسارات (~90% تقليص)، فهارس مركّبة مقصودة، WAL، وسياسة احتفاظ. لكن:
- **حجب حلقة الأحداث:** `feedparser.parse` (`rss_feeds.py:173`، `iran_osint.py:169`) تحليل XML متزامن (10-150ms) على خيط الحلقة، فكل طلب API متزامن يُحجَب. **الحل:** `await asyncio.to_thread(feedparser.parse, ...)`.
- **جلب الخلاصات تسلسلي:** `rss_feeds.py:150-157` — 16 خلاصة × مهلة 20s = حتى 320s مقابل فاصل 120s، و`max_instances=1` يُسقط الدورات المتداخلة فتنخفض معدلات الجمع بصمت. **الحل:** `asyncio.gather` مع `Semaphore(6)`.
- **N+1 في `/api/collectors/status`** (`main.py:212-222`) — 10 استعلامات تسلسلية في حلقة، رغم أن الفهرس المركّب في `database.py:72` أُضيف لهذا الغرض تحديداً.
- **فهارس ناقصة:** `IranianLeaderNews.collected_at` (تستخدمه وظيفة الاحتفاظ) و`(is_military, tracked_at)` المركّب.
- **`DateTime` بلا `timezone=True`** (`database.py:53-54`) — القيم تُقرأ naive، والكود يصلحها يدوياً (`main.py:225-228`)، لكن سيُنتج نتائج خاطئة على PostgreSQL الذي يدّعي دعمه.
- **حصة NewsAPI ستُستنزف:** 12 طلباً/دورة كل 600s = 1,728/يوم مقابل حد 100/يوم (`news_api.py:44-45`).

### 2.6 وجود الاختبارات وتغطيتها — **5.5/10**

12 ملف اختبار باك إند (~879 سطراً، ~95 دالة) + 8 ملفات واجهة (~664 سطراً). **تغطية ممتازة:** `dates`، `adsb`، `security`، `api/iran`. **تقدير التغطية: ~55-60%.** مشاكل:
- **`test_main.py:40-49` يضرب ~30 نقطة خارجية حية** ويكتب صفوفاً حقيقية في قاعدة الاختبار، ما يلوّث `test_events.py::test_country_index`، ويحرق حصة NewsAPI عند كل push. التأكيد `in (200, 429)` بديهي (يمر مهما حدث).
- **~450 سطراً من الجامعين بلا تغطية** (`rss_feeds`، `gdelt`، `news_api`، `ucdp`).
- **`test_database.py:67-76` اختبار يُستخدم كـteardown** فيخلق تبعية ترتيب.
- **تأكيدات فضفاضة/فارغة:** `test_infrastructure.py:14` (`>= 15` بينما التعليق يقول 19)؛ `test_events.py:18-23` يمر فارغاً إذا فشل البذر بصمت.
- لا اختبار سلبي واحد (500/timeout/XML مشوّه/فشل commit)، ولا حد أدنى للتغطية في CI (`--cov` بلا `--cov-fail-under`).

### 2.7 التوثيق — **7.5/10**

فوق المتوسط: كل وحدة لها docstring، وأغلبها يشرح **لماذا** لا **ماذا** (`ratelimit.py:1-10` يذكر نموذج التهديد والخوارزمية وحدودها؛ `auth.py:1-10` يشرح التصميم المفتوح افتراضياً). تعليقات مبرّرات التغيير حاضرة (`BUG-1`، `PERF-3`) فيصبح الكود قابلاً للتدقيق ذاتياً. لكن:
- **4 تعليقات خاطئة فعلياً** (أسوأ من غيابها): `main.py:107-108`، `scheduler.py:47`، `database.py:143`، `ratelimit.py:37`.
- **لا Pydantic response models في أي مكان** — `/docs` يعرض مخطط كل استجابة كـ `{}` فارغ، وهو أغلى فجوة توثيق لواجهة تستهلكها React منفصلة.
- **أعقد دالتين لهما أنحف docstrings** (`iran_osint.py:160` بلا docstring إطلاقاً).

### 2.8 اتفاقيات التسمية ونمط الكود — **7.0/10**

اتفاقيات قوية ومتسقة (بادئة `_private`، عقد الجامع الموحّد `collect_<source>() -> int`، بناء router موحّد، أسماء logger موحّدة). لكن:
- **جيلان من التلميح النوعي متعايشان:** `Optional[str]`/`Dict`/`List` القديم مقابل `str | None`/`list[str]` الحديث، رغم أن الهدف `py311`. **الحل:** فعّل ruff `UP` + `--fix`.
- **غياب تلميحات النوع من كل معالجات المسارات**، بينما `processors/*` مُلمَّح بالكامل.
- **نماذج SQLAlchemy بأسلوب 1.x** (`Column(...)`) تحت تثبيت 2.0 — فلا أنواع ثابتة على سمات النموذج (ولهذا يبقى باگ naive-datetime غير مرئي).
- **`== True`** في `flights.py:32,66` مع إعفاء lint بمبرّر خاطئ (`.is_(True)` صحيح ونظيف).
- **`source_id` يُبنى بطريقتين:** ثلاثة جامعين يجزّئون URL بينما `gdelt.py:52` يستخدم URL خاماً مقتطعاً (تصادم محتمل).

---

## 3. المظهر العام والتصميم وتجربة المستخدم UX/UI (وزن 30%) — **5.5/10**

### 3.1 الاتساق البصري — **6.0/10**

الهوية (سماوي على كحلي داكن) متماسكة ونمط البطاقة مكرّر بثبات. لكن:
- **10 من 13 رمز ثيم كود ميت:** `tailwind.config.js:9-23` يعرّف `rasad.*` مع تعليق صريح "استخدمها بدل الـhex"، لكن grep لـ`rasad-accent|rasad-danger|...` يعيد **صفر نتائج**. المكوّنات تثبّت الألوان الخام (68× `text-slate-300`، 23× `text-cyan-400`). لون الإبراز موجود بـ4 تهجئات بلا قاعدة.
- **4 مقاييس أخضر→أحمر متنافسة لنفس المفهوم:** `Header.jsx:78-84` (عتبات 30/15) مقابل `StatsPanel.jsx:15` (نفس العتبات، ألوان hex مختلفة) مقابل `CountryIndex.jsx:12-17` (عتبات 75/50/25) مقابل `constants.js:23-28`. فمؤشر التصعيد يظهر `yellow-400` في الهيدر و`#f59e0b` في الإحصائيات **لنفس القيمة**. **الحل:** `scoreColor()` واحدة مُصدَّرة.
- **خط خارج المقياس:** `text-[11px]` مستخدم **45 مرة** مقابل 52× `text-xs`.
- **ثلاث قيم "رسمية" للون الخلفية:** `#0a0e17` (tailwind) مقابل `#0d1117` (theme-color في index.html) — splash الـPWA وشريط العنوان لن يتطابقا.
- **z-index مخصّص مع باگ تكديس فعلي:** درج TV (9999) فوق نافذة التنبيهات (2000) وباكدروبها، فالنقر عليه لا يغلق النافذة.
- **CSS ميت:** `.dashboard-grid`، `.escalation-gauge`، `.panel`، `.flight-icon`، `animate-flash` — صفر مراجع.

### 3.2 سهولة الاستخدام ووضوح التنقل — **5.0/10**

تبويبات ARIA سليمة ومبدّل جوال ذكي، لكن:
- **🔴 وضع 3D يفقد كل تحكمات الخريطة** — مفاتيح الطبقات، الزوم، إعادة التمركز، والمفتاح كلها داخل `RasadMap`، و`App.jsx:176-206` يستبدله كلياً بـ`RasadGlobe` الذي يعيد `<div>` فارغاً. فلا طريقة لتبديل طبقة أو قراءة المفتاح في 3D. **الحل:** ارفع chrome التحكم إلى `App.jsx`.
- **لا استمرارية للحالة في الرابط:** فقط `viewMode` ينجو من إعادة التحميل؛ `filters`/`activeTab`/`selectedEvent` عابرة، فمحلل يصفّي `country=IR, severity=critical` لا يستطيع الإشارة المرجعية أو المشاركة، وF5 يعيد كل شيء.
- **نقطتا دخول شبه خفيتين:** درج TV شريحة 30px بلا تسمية مرئية إلا عند التحويم (`LiveTVDrawer.jsx:106-114`)؛ زر طي اللوحة تبويب 24px بلا تسمية، وأيقونته معكوسة دلالياً (`App.jsx:379`).
- **النوافذ لا تُغلق بـEscape ولا إدارة تركيز** (لا `AlertSettings` ولا `LiveTVDrawer`).
- **تبويب إيران يكرّر جلبات App** بحدود مختلفة (100 مقابل 50) فيرى المستخدم عدّين مختلفين لنفس التدفق.

### 3.3 الاستجابة عبر الشاشات — **4.0/10** (أضعف بُعد)

- **🔴 الهيدر بلا أي breakpoints ويُقصّ على الجوال:** `Header.jsx:53-54` صف واحد بلا `sm:`/`md:`/`flex-wrap` يجب أن يسع ~700-800px من المحتوى، والجذر `overflow-hidden` (`App.jsx:273`) فيُقصّ لا يُمرَّر — على شاشة 375px تختفي الساعة وحالة الاتصال والجرس (وفي RTL يُقصّ الشعار). **الحل:** `flex-wrap` + إظهار تدريجي (`hidden lg:flex` على الرقائق).
- **Leaflet لا يُخبَر بتغيّر حجم حاويته:** grep لـ`invalidateSize` = صفر. طيّ اللوحة يغيّر العرض 360-500px فتبقى بلاطات رمادية ومركز منزاح. لاحظ أن مسار 3D يصيبها (`RasadGlobe.jsx:84-89` يستخدم `ResizeObserver`). **الحل:** انسخ النمط.
- **لا معالجة safe-area** رغم `viewport-fit=cover` (`index.html:5`) و`display: standalone` — أزرار القنوات تجلس تحت مؤشر الصفحة الرئيسية على iPhone.
- **لوحة الطبقات `max-w-[45vw]`** على 375px = 168px لستة صفوف تلتف وتأكل ~40% من ارتفاع الخريطة.

### 3.4 دعم العربية وRTL — **6.0/10**

**أفضل جزء في الأساسيات:** `i18n/index.js:30-39` يضبط `lang` و`dir` على `<html>` ويعيد التطبيق عند `languageChanged`؛ `index.html:2` يشحن `dir="rtl"` فلا وميض LTR؛ `ar.json:196-213` ينفّذ **مجموعة الجمع العربية الكاملة** (`_one/_two/_few/_many`). لكن:
- **🟠 `ar-SA` ينتج تواريخ هجرية وأرقاماً هندية:** `Header.jsx:11` و`Timeline.jsx:35` — يحلّ إلى تقويم أم القرى وأرقام هندية، فيقرأ الهيدر `الأربعاء، ٥ صفر ١٤٤٨ هـ` بينما كل رقم آخر لاتيني (نظاما ترقيم متجاوران). **الحل:** `'ar-SA-u-ca-gregory-nu-latn'`.
- **نص لاتيني في مقاطع RTL بلا عزل ثنائي الاتجاه:** أسماء المصادر/الدول تُدرَج في جمل عربية (`NewsFeed.jsx:213-214`، `Header.jsx:63`) فتقفز النقطة الفاصلة. **الحل:** `<bdi>`.
- **6 خصائص CSS فيزيائية متبقية** (الكود غالباً منطقي): `App.jsx:377` (`left-0 border-l-0 rounded-r-lg` — تبويب الطي ينفصل ويطفو على الجانب الخطأ في الإنجليزية)؛ `StatsPanel.jsx:65,89` (`text-left`)؛ `CountryIndex.jsx:61-64` (`ml-1`).
- **`RasadGlobe` يثبّت `direction:rtl`** في كل tooltip (`:117,134,150,163`) ولا يستورد `useTranslation` إطلاقاً، بينما `RasadMap.jsx:1-6` يوثّق إصلاح هذا الباگ بالضبط عبر `i18n.dir()` — الإصلاح لم يُطبَّق على الكرة.
- **🟠 باگ:** `RasadGlobe.jsx:135` يطبع `undefined` للثقة (`CONFIDENCE` بلا مفتاح `label` — نُقلت للـi18n عمداً، `constants.test.js:35` يؤكد ذلك). فكل tooltip ضربة إيرانية على الكرة يعرض `🟡 undefined`.
- **باگ:** سحب درج TV معكوس في RTL (الافتراضي) — `insetInlineStart` منطقي مع إحداثيات مؤشر فيزيائية (`LiveTVDrawer.jsx:93-100`).

### 3.5 إمكانية الوصول — **6.0/10**

استثمار حقيقي: حلقة `:focus-visible` عالمية مستخدمة 30×، skip link، `prefers-reduced-motion`، `fieldset`/`legend`، مدخلات مُعنونة، معالجات لوحة مفاتيح على كل صف. لكن:
- **لا `<main>` landmark**، وskip link يستهدف `<div>` غير قابل للتركيز (`App.jsx:274→385`) فيفشل صامتاً في Safari/Firefox.
- **نافذة التنبيهات ليست dialog:** تنقص `role="dialog"`، `aria-modal`، Escape، فخ التركيز، واستعادة التركيز (`AlertSettings.jsx:48-57`) — مستخدم لوحة المفاتيح يخرج بـTab للوحة المعتمة خلفها.
- **محتوى تفاعلي متداخل:** `<a href>` داخل `role="button"` (`NewsFeed.jsx:186-223`، `IranPanel.jsx` يعشّش رابطين) — ARIA غير صالح.
- **منزلق العتبة بلا اسم وصول** (`AlertSettings.jsx:157-172`)؛ **9 أزرار تبديل بلا `aria-pressed`** (`AlertSettings.jsx:102-144`) رغم أن النمط مطبّق صحيحاً في `Timeline.jsx` و`App.jsx`.
- **لا منطقة حيّة (live region)** للوظيفة الأساسية — مستخدم قارئ الشاشة لا يُعلَم بحدث حرج جديد.
- **45 نسخة من نص 11px** لمحتوى (لا chrome فقط) — في العربية يفقد Tajawal تمييز أشكال الحروف (ب/ت/ث) عند 11px.
- **لا `eslint-plugin-jsx-a11y`** (`eslint.config.js:18-21`) — كل ملاحظات هذا القسم كانت ستُكتشف آلياً.

### 3.6 التغذية الراجعة للمستخدم — **6.0/10**

toast ممتاز (`role=status` + إخفاء تلقائي + زر إغلاق)، بانر بيانات قديمة أمين، عدّاد `Retry-After` للـ429، و`ErrorBoundary` يهبط 3D→2D. لكن:
- **علم `loading` مُهمَل في كل مواضع الاستطلاع الـ11:** `usePolling.js:61` يعيد `loading` لكن `App.jsx:63-108` لا يأخذه ولا مرة، فعلى أول رسم `NewsFeed`/`Timeline`/`CountryIndex` تعرض **"لا توجد أحداث"** بينما الطلب لا يزال جارياً. `StatsPanel.jsx:12` وحده يصيبها بـ`.shimmer`. وتبويب القادة يعرض لوحة **فارغة بيضاء** أثناء التحميل (`IranPanel.jsx:218-261`).
- **إجراء مدمّر بلا تأكيد/تراجع:** زر "مسح" يمسح `recentAlerts` فوراً بلا استرجاع (`AlertSettings.jsx:198-203`).
- **تفاصيل الخطأ تُسطَّح لسلسلة:** `usePolling.js:32` يخزّن `err.message`، فتُفقد بيانات `ApiError` (status/retryAfter) لكل نقطة مستطلَعة — 401 و429 وانقطاع الشبكة لا تُميَّز، ولا زر إعادة محاولة في `NewsFeed` (بينما `IranPanel` له واحد).
- **النوافذ المنبثقة للخريطة تُدمَّر كل 30 ثانية:** تأثير `RasadMap.jsx:131-225` يعتمد على `events` التي تتغيّر هويتها كل استطلاع، فيبدأ بـ`clearLayers()` — أي نافذة مفتوحة تُدمَّر والعناقيد تنطوي كل نصف دقيقة.

### 3.7 جودة هندسة React (ملاحظات إضافية)

- **`App.jsx` طبقة بيانات 409 سطر:** 11 حلقة استطلاع مستقلة = 11 مؤقّت + 11 مستمع `visibilitychange` + 11 طلباً متزامناً على mount (المتصفح يحدّها بـ6/مضيف).
- **مصفوفة deps منتشرة** (`usePolling.js:59` — `[..deps]`) انتهاك ثابت لـReact، آمن اليوم لكنه على بُعد refactor من الانهيار.
- **لا حدود memoization:** لا `React.memo` في أي مكان، فكل استطلاع يعيد رسم الشجرة كاملة.
- **تظليل متغيّر:** `t` مترجِم ومعرّف مؤقّت معاً (`Header.jsx:9,36`).
- **مفاتيح الفهرس في قوائم تُضاف من الأعلى** (`AlertSettings.jsx:210` — القائمة تُضاف بالبادئة فتنزاح كل الفهارس).
- **`attributionControl: false`** (`RasadMap.jsx:104`) يزيل إسناد CARTO/OSM الذي **تتطلّبه شروطهما** — مسألة قانونية في لوحة عامة.
- **قوام الكرة من CDN طرف ثالث** (`RasadGlobe.jsx:68-69` — `//unpkg.com`) بلا احتياطي؛ ومعرّفات بث YouTube مثبّتة (`LiveTVDrawer.jsx:13-15`) ستنكسر بصمت عند إعادة تشغيل أي قناة.

---

## 4. المراجعة الأمنية (Cybersecurity) (وزن 25%) — **6.0/10**

> **ملاحظة:** الشجرة العاملة الحالية في حالة جيدة فعلاً بعد جولتي إصلاح أمني سابقتين. أغلب الخطر المتبقي في **تاريخ Git** وفي **افتراضات حدود الثقة عند النشر**، لا في الكود الحالي.

### 4.1 تخزين المفاتيح السرية — **4/10**

- **🔴 حرج — مفاتيح حية قابلة للاستخراج من تاريخ Git:** `.env` أُودِع في `271d6e2` وحُذف من الشجرة في `f565426`، لكن **الحذف لا يزيله من التاريخ**:
  ```
  git show 271d6e2:.env
  newsapi_key=2f4ea22b7b394a10a4ce2e9ed67d14c7
  OPENSKY_CLIENT_SECRET=FrpyQsOO3DYZIwiFk0MXqRzaC6ChzVdc
  ```
  مؤكَّد الوصول اليوم: `git log --all -S "FrpyQsOO..."` → `f565426, 271d6e2`. **الحل:** التدوير أولاً (من لوحتي NewsAPI وOpenSky)، ثم إعادة كتابة التاريخ بـ`git filter-repo`، ثم تفعيل Secret Scanning + Push Protection.
- **🔴 حرج — روابط Google Alerts (معرّف حساب) في التاريخ:** `4baa5c1` و`dce34df` ثبّتا 14+ رابط خلاصة، ومقطع المسار `03099333582095282205` معرّف حساب مستقر — من يملك الروابط يقرأ تدفق تنبيهات المالك بلا إبطال سوى حذف التنبيهات.
- **قوي جداً:** `SECURITY.md:24-46` **يوثّق التسريب صراحةً** ويسمّي الـcommit ويعطي إعادة الإنتاج الدقيقة ويرتّب التدوير قبل التاريخ — قسم إفصاح نموذجي (لكنه موثَّق لا مُعالَج بعد). `.env.example` **لا يشحن أي قيم افتراضية** (لا footgun مفتاح افتراضي). grep كامل للأسرار: صفر نتائج في الشجرة الحالية.

### 4.2 ثغرات الحقن — **9/10**

- **SQL injection: لا شيء.** كل استعلام يستخدم SQLAlchemy 2.0 بمعاملات مربوطة؛ لا `text()` ولا f-string SQL في أي مكان. الـSQL الخام الوحيد ثلاثة PRAGMA ثابتة بلا مدخلات.
- **قوي بشكل ملحوظ — حقن LIKE مغلَق:** `events.py:43-49` يستخدم `contains(search, autoescape=True)` فلا يستطيع المستخدم فرض `%` لمسح كامل الجدول.
- **XSS: مدافَع عنه جيداً** — محتوى الأخبار الخارجي يمرّ عبر `esc()` عند كل sink (مُتحقَّق في `RasadMap.jsx` و`RasadGlobe.jsx`)، والروابط عبر `safeUrl()` أولاً (`security.test.js` يؤكد رفض `javascript:`/`data:`/`file:`). لا `dangerouslySetInnerHTML` ولا `eval`.
  - **🟠 متوسط:** `esc()` ليست آمنة-السمات بالبناء بل بالاتفاق — `RasadMap.jsx:156` يُدرِج `${cat.color}` بلا تهريب في `style=` (آمن اليوم لأن المصدر ثابت، ينكسر لو صار اللون من الخادم). **الحل:** الهجرة إلى بناء DOM (`textContent`).
- **حقن أوامر: لا شيء** — ملفات `.bat` و`run_desktop.py` تستخدم سلاسل حرفية ثابتة، لا `subprocess`/`shell=True`.
- **XXE: غير مُستغَل** — `feedparser` يعطّل الكيانات الخارجية افتراضياً (تخفيف بالمكتبة لا بكود التطبيق).

### 4.3 التحقق من المدخلات — **7/10**

- **قوي:** كل معامل صفحات/نافذة رقمي مقيّد بـ`ge`/`le` (`events.py:20-23`، `flights.py:21-22`) — لا `limit` أو `hours` غير محدود.
- **🟠 متوسط:** فلاتر السلاسل بلا `max_length` ولا `Literal` (`events.py:16-19` — `category`، `severity`)، فيمكن إرسال قيم بميغابايتات (غير قابلة للحقن لكنها تُربَط في الاستعلامات وتُردَّد في مسارات الخطأ). **الحل:** `max_length=50` + `Literal[...]`.
- **🔵 بسيط:** `str(exception)` يُعاد للعميل في `/api/refresh` (`main.py:172-173`) — نصوص httpx قد تحوي URLs، و`news_api.py:52` يضع `apiKey` في query string. **الحل:** سجّل التفصيل، أعِد رسالة عامة.
- **مسارات المسار نظيفة** — لا traversal ممكن (كلها تحلّ ملفات ثابتة نسبةً لـ`Path(__file__)`).

### 4.4 إدارة الصلاحيات والمصادقة — **3/10**

- **13 من 14 نقطة بلا أي مصادقة:** `require_api_key` مستخدم في مكان واحد فقط (`main.py:138` على `/api/refresh`). كل نقاط القراءة مفتوحة — لأداة تجمّع قواعد عسكرية ومنشآت نووية ومسارات طيران عسكري، "من يصل للمنفذ يقرأ كل ما جمعته" تعرّض حقيقي. لا نموذج تخويل إطلاقاً.
- **مخطط المفتاح نفسه سليم:** `compare_digest` بزمن ثابت، 401 صحيح، لا تسجيل للمفتاح (`auth.py:20-31`).
- **🟠 متوسط — fail-open صامت:** `auth.py:23-24` يعيد `None` عند مفتاح فارغ (مقايضة سطح المكتب الموثّقة)، لكن من يضبط `BACKEND_HOST=0.0.0.0` وينسى `API_KEY` يحصل على API مفتوح كلياً بلا تحذير. **الحل:** سجّل `WARNING` عند بدء التشغيل إذا كان host غير loopback والمفتاح فارغ.
- **ضوابط تعويضية جيدة:** افتراض `127.0.0.1`، ربط compose على loopback، cooldown 120s على `/api/refresh` مستقل عن المفتاح، توثيق `SECURITY.md`.

### 4.5 التعامل الآمن مع البيانات والنقل — **5/10**

- **🟠 متوسط — لا TLS في أي طبقة:** `nginx.conf:2` على 8080 عادي؛ HSTS معلّق (`:20-21`)؛ CORS الافتراضي كله `http://`. مقبول لـloopback؛ لكن عند النشر يعبر كل شيء (بما فيه `X-API-Key`) نصاً صريحاً.
- **🟠 متوسط — مفتاح API يُشحن في حزمة العميل:** `api.js:13` `import.meta.env.VITE_API_KEY` — أي `VITE_*` **يُدمَج في JS المبني** فيصير عاماً. لمسار GitHub Pages، ضبطه ينشر مفتاح الخادم للعالم. **الحل:** وثّق صراحةً أنه لبناء محلي/سطح مكتب فقط (النشر الحالي لا يمرّره — فخ كامن لا تسريب حي).
- **🔵 بسيط:** قاعدة البيانات بلا تشفير عند الراحة؛ `Cache-Control: public` على كل استجابات API (آمن اليوم لأن لا استجابة خاصة بمستخدم).
- **قوي:** سياسة احتفاظ مُنفَّذة تحدّ نصف قطر الانفجار؛ `Referrer-Policy: no-referrer`؛ رفض روابط Google Alerts غير HTTPS؛ كل طلبات الجامعين `https://` حصراً.

### 4.6 الاعتماديات — **8/10**

- تثبيت دقيق في النظامين، وتحقّق حي أثناء المراجعة: starlette/pydantic/sqlalchemy/httpx على **أحدث** إصدار؛ القليل متأخر بسيطاً (feedparser 6.0.11→6.0.13، aiosqlite 0.20→0.22).
- `requirements.txt:4-6` يوثّق سبب تثبيت starlette صراحةً (التثبيت السابق fastapi 0.115 قفل starlette 0.38.6 بـ7 ثغرات) — الإصلاح الصحيح تماماً.
- **قوي:** فصل تشغيل/تطوير؛ `pip-audit --strict` + `npm audit` في CI؛ overrides للحزم المعروفة الثغرات (`ejs@3.1.10`)؛ Dependabot عبر 4 أنظمة؛ `npm ci` للبناء المستنسخ.
- **🔵 بسيط:** صور القاعدة مثبّتة بالوسم لا بالبصمة (`python:3.12-slim`)؛ `ci.yml` بلا `permissions:` block صريح (مخفَّف: `pull_request` لا `pull_request_target`)؛ مُرحّل Nitter (`nitter.poast.org`) موسوم ثقة `HIGH` رغم كونه مثيلاً تطوعياً بلا ضمان سلامة.

### 4.7 إعدادات CORS وترويسات الأمان — **7/10**

- **CORS مقيّد صحيحاً:** `main.py:95-102` — قائمة أصول صريحة من الإعدادات، `allow_credentials=False`، طرق مضيّقة (يغلق SEC-3 السابق: `*` مع credentials).
- **CSP قوي ومتطابق web↔desktop:** `nginx.conf:14` و`tauri.conf.json:30` سياسة شبه متطابقة عمداً مع `object-src 'none'`، `frame-ancestors 'none'`.
  - **🟠 متوسط:** `script-src 'unsafe-inline' 'unsafe-eval'` يُضعف CSP كتخفيف XSS جوهرياً.
- **🟠 متوسط — لا ترويسات أمان على GitHub Pages:** `deploy-frontend.yml` ينشر إلى Pages حيث **لا يُستخدم `nginx.conf`**، و`index.html` **بلا meta CSP احتياطي** — فالبناء المنشور علناً هو الوحيد بلا حماية ترويسات وهو يعرض محتوى خلاصات غير موثوق. **الحل:** أضِف `<meta http-equiv="Content-Security-Policy">` لـ`index.html`.
- **🟠 متوسط — وراثة `add_header` مكسورة في nginx:** `location /assets/` (`:44-49`) يُسقط كل الترويسات الموروثة على مستوى الخادم ويعيد اثنتين فقط (يحذف CSP، X-Frame-Options)؛ نفس العيب في `= /sw.js`.
- **🟠 متوسط — X-Forwarded-For موثوق بلا شرط:** `ratelimit.py:36-41` + `nginx.conf:33` يستخدم `proxy_add_x_forwarded_for` الذي **يُلحِق** بقيمة العميل، والـmiddleware يأخذ `split(",")[0]` = القيمة المتحكَّم بها. تدوير الرأس يهزم الحدّ كلياً. **الحل:** استخدم `$remote_addr` + خلف `trusted_proxies` افتراضه off.
- **🔵 بسيط:** استجابات 429 بلا ترويسات CORS (نتيجة ترتيب middleware المعكوس — انظر 2.4).
- **حاويات ممتازة:** non-root صحيح (`backend/Dockerfile:12`)، `nginx-unprivileged`، بناء متعدد المراحل، `--reload` في صورة التطوير فقط.

---

## 5. الدرجة الإجمالية المرجّحة

**المعادلة:** الإجمالي = (هندسة × 0.45) + (UX/UI × 0.30) + (أمن × 0.25)
= (6.5 × 0.45) + (5.5 × 0.30) + (6.0 × 0.25) = 2.925 + 1.65 + 1.5 = **6.075 ≈ 6.1/10**

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 6.5/10 | 45% | 2.93 |
| UX/UI | 5.5/10 | 30% | 1.65 |
| أمن سيبراني | 6.0/10 | 25% | 1.50 |
| **الإجمالي** | | | **6.1/10** |

**الخلاصة:** مشروع شخصي طموح ومنجز بانضباط هندسي مرئي (مهلات شاملة، طبقة تواريخ موحّدة، upsert ذرّي، فهارس مقصودة، احتفاظ، rate limiting، ETag، CI حقيقي، تعليقات مبرّرات). أفضل بوضوح من مراجعته السابقة. ما يفصله عن الجاهزية الإنتاجية مُركَّز في: **(1)** طبقة الجامعين (غير مُختبَرة، مكرّرة، عاجزة بنيوياً عن التبليغ عن الفشل)؛ **(2)** صحّة البنية عند الحدود (ترتيب middleware المعكوس، DateTime naive، لا migrations)؛ **(3)** الاستجابة على الجوال (الهيدر مكسور) والأسرار في تاريخ Git.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع بعد الحل |
|---|---|---|---|---|---|
| 1 | حرج 🔴 | أمن | مفاتيح حية (NewsAPI/OpenSky) + 14 رابط Google Alerts في تاريخ Git (`271d6e2`, `4baa5c1`) | دوّر المفاتيح فوراً؛ احذف/أعِد إنشاء التنبيهات؛ أعِد كتابة التاريخ بـ`git filter-repo`؛ فعّل Secret Scanning + Push Protection | إغلاق تسريب اعتماد دائم؛ منع اختطاف الحسابات |
| 2 | حرج 🔴 | UX/استجابة | الهيدر بلا breakpoints يُقصّ على الجوال (`Header.jsx:53` + `overflow-hidden` في `App.jsx:273`) | `flex-wrap gap-y-1` + إظهار تدريجي (`hidden lg:flex` رقائق، `hidden md:block` تاريخ) | الوصول لكل عناصر التحكم على الهاتف بدل قصّها |
| 3 | حرج 🔴 | هندسة | ترتيب middleware معكوس: 429 بلا CORS (`main.py:95-113`) | سجّل `CORSMiddleware` أخيراً؛ صحّح التعليق؛ أضِف اختبار تراجع | الواجهة تقرأ `Retry-After` بدل خطأ شبكة معتم |
| 4 | حرج 🔴 | أمن | تجاوز rate-limit عبر X-Forwarded-For (`ratelimit.py:36` + `nginx.conf:33`) | استخدم `$remote_addr` أو الإدخال الأيمن؛ خلف `trust_proxy_headers` افتراضه off | استعادة فعالية حدّ المعدل ضد الإغراق |
| 5 | مهم 🟠 | هندسة | الجامعون يحوّلون كل فشل إلى 0 (`gdelt.py:94-98` وأشباه) فيبدو المعطّل كالهادئ | أعِد `CollectResult(count, error)` أو ارفع الاستثناء ليصله `gather` | `/api/collectors/status` يصير صادقاً؛ كشف الجامع المعطّل |
| 6 | مهم 🟠 | هندسة/أداء | `feedparser.parse` يحجب حلقة الأحداث + جلب تسلسلي يتجاوز الفاصل (`rss_feeds.py:173,150-157`) | `await asyncio.to_thread(...)` + `asyncio.gather` مع `Semaphore(6)` | إلغاء تجمّد الـAPI؛ التزام معدل الجمع بالجدولة |
| 7 | مهم 🟠 | هندسة | لا migrations؛ ترقية سطح المكتب تكسر `no such column` (`database.py:151`) | Alembic أو خطوة `ALTER TABLE` في `init_db` | ترقيات آمنة لمستخدمي المثبّت |
| 8 | مهم 🟠 | UX/RTL | `ar-SA` يعرض تواريخ هجرية وأرقاماً هندية (`Header.jsx:11`, `Timeline.jsx:35`) | `'ar-SA-u-ca-gregory-nu-latn'` + توحيد نظام الترقيم | تواريخ ميلادية متسقة للمحلّل |
| 9 | مهم 🟠 | UX | وضع 3D يفقد كل تحكمات الخريطة والطبقات (`App.jsx:176-206`) | ارفع chrome التحكم (طبقات/زوم/مفتاح) إلى `App.jsx` كأشقّاء | تكافؤ وظيفي بين 2D و3D |
| 10 | مهم 🟠 | UX | علم `loading` مُهمَل في 11 موضع فتظهر "لا أحداث" أثناء التحميل (`App.jsx:63-108`) | مرّر `loading` + skeletons؛ احرس تبويب القادة | تمييز التحميل من الفراغ من الخطأ |
| 11 | مهم 🟠 | UX/أداء | نوافذ الخريطة تُدمَّر كل 30 ثانية (`RasadMap.jsx:131-225`) | صالِح العلامات بالـid بدل `clearLayers()` | بقاء النوافذ المفتوحة أثناء الاستطلاع |
| 12 | مهم 🟠 | UX/استجابة | Leaflet لا يُخبَر بتغيّر الحجم (لا `invalidateSize`) | أضِف `ResizeObserver` (انسخ `RasadGlobe.jsx:84-89`) | خريطة صحيحة بعد طيّ اللوحة |
| 13 | مهم 🟠 | أمن | لا CSP على GitHub Pages؛ وراثة ترويسات nginx مكسورة (`index.html`, `nginx.conf:44-55`) | أضِف meta CSP لـ`index.html`؛ أكمل مجموعة ترويسات كل location | حماية ترويسات على البناء المنشور علناً |
| 14 | مهم 🟠 | هندسة | باگ `"ground"` في عمود Float + رموز طوارئ تُصنَّف عسكرية (`adsb.py:194,35`) | حوّل "ground"→0؛ أزل 7500/7600/7700 من الكشف العسكري + صحّح الاختبار | بيانات طيران صحيحة؛ إزالة إيجابيات كاذبة |
| 15 | مهم 🟠 | هندسة | `DateTime` بلا `timezone=True` سيكسر PostgreSQL (`database.py:53-54`) | `Column(DateTime(timezone=True))` واحذف الالتفاف اليدوي | نتائج صحيحة على مسار PostgreSQL المدعوم |
| 16 | مهم 🟠 | اختبارات | `test_main.py:40-49` يضرب ~30 نقطة خارجية حية ويلوّث قاعدة الاختبار | `monkeypatch` الجامعين الخمسة بـ`AsyncMock` | إزالة التقطّع وحرق الحصة وتلوّث الاختبارات |
| 17 | مهم 🟠 | أمن/وصول | نافذة التنبيهات ليست dialog (لا Escape/فخ تركيز) (`AlertSettings.jsx:48`) | `role="dialog" aria-modal` + Escape + فخ/استعادة تركيز + أضِف `jsx-a11y` | وصول لوحة مفاتيح صحيح للنوافذ |
| 18 | تحسين 🟡 | جودة كود | تكرار ~60 سطر بين `_process_feed`/`_process_iran_feed` | استخرج `_feed_base.py` بـ`mapper` نقي قابل للاختبار | إزالة تكرار + إغلاق فجوة تغطية |
| 19 | تحسين 🟡 | بصري | 10/13 رمز ثيم ميت + 4 مقاييس ألوان متنافسة + z-index بباگ تكديس | احذف/فعّل الرموز؛ `scoreColor()` مُصدَّرة؛ سلّم z-index في الثيم | اتساق بصري + إصلاح تكديس TV فوق النافذة |
| 20 | تحسين 🟡 | RTL | 6 خصائص فيزيائية + globe `undefined` + سحب TV معكوس | خصائص منطقية؛ `useTranslation` للكرة؛ إحداثيات فيزيائية للسحب | RTL متسق + إصلاح باگّي العرض والسحب |
| 21 | تحسين 🟡 | قانوني/أصول | `attributionControl: false` يزيل إسناد CARTO/OSM المطلوب؛ قوام كرة من CDN؛ معرّفات بث مثبّتة | أعِد الإسناد؛ استضِف القوام محلياً؛ انقل المعرّفات للإعدادات | التزام شروط المزوّدين + متانة التحميل |
| 22 | تحسين 🟡 | جودة/توثيق | جيلا تلميح نوعي + غياب response models + تعليقات خاطئة | ruff `UP`؛ Pydantic response models؛ صحّح التعليقات الأربعة | `/docs` يوثّق العقد + كود قابل للتدقيق |

---

## 7. حالة التنفيذ (نُفِّذت خطة الإصلاح في هذا الـ PR)

نُفِّذت بنود الخطة أعلاه على هذا الفرع. جميع اختبارات الباك إند (pytest) والواجهة
(vitest) وruff وESLint والبناء الإنتاجي تمرّ خضراء.

| # | البند | الحالة |
|---|------|--------|
| 1 | أسرار في تاريخ Git | ⚠️ **يدوي** — التدوير يتطلّب لوحات المزوّدين وتنظيف التاريخ force-push مدمّر (موثّق في `SECURITY.md`). صُلِّب `.gitignore` (`.env.*`) ووُثِّق خطر `VITE_API_KEY`. |
| 2 | الهيدر مستجيب | ✅ `flex-wrap` + إظهار تدريجي |
| 3 | ترتيب CORS/429 | ✅ CORS الأبعد + اختبار تراجع |
| 4 | X-Forwarded-For | ✅ `trust_proxy_headers` (off) + nginx `$remote_addr` |
| 5 | فشل الجامعين | ✅ يرفعون الفشل + `_feed_base` |
| 6 | feedparser غير حاجب | ✅ `asyncio.to_thread` + `Semaphore` |
| 7 | migrations | ✅ `ALTER TABLE` في `init_db` |
| 8 | ar-SA ميلادي | ✅ `-u-ca-gregory-nu-latn` |
| 9 | تحكمات 3D | ✅ `LayerToggles` مشترك + طبقة طيران للكرة |
| 10 | حالات loading | ✅ تمرير `loading` + skeletons |
| 11 | تصالح العلامات | ✅ حارس توقيع يمنع تدمير النوافذ |
| 12 | invalidateSize | ✅ `ResizeObserver` |
| 13 | CSP/ترويسات nginx | ✅ meta احتياطي + إكمال ترويسات location |
| 14 | باگّا adsb | ✅ `"ground"`→0 + رموز الطوارئ علَم منفصل + اختبارات |
| 15 | DateTime tz | ✅ `DateTime(timezone=True)` + فهارس |
| 16 | test_main حي | ✅ `monkeypatch` الجامعين |
| 17 | dialog a11y | ✅ role/aria-modal/Escape/تركيز + `<main>` landmark |
| 18 | تكرار الخلاصات | ✅ استُخرج `_feed_base` |
| 19 | رموز ثيم/scoreColor | ✅ توحيد + حذف الميت |
| 20 | RTL فيزيائي/globe/TV | ✅ خصائص منطقية + globe مترجَم + سحب TV مُصلَح |
| 21 | إسناد الخريطة | ✅ إسناد CARTO/OSM (قوام CDN مؤجّل — 🟡) |
| 22 | تلميح نوعي/تعليقات | ◑ جزئي: `.is_(True)` + تصحيح تعليقات + توحيد `source_id`؛ response models مؤجّلة (🟡) |

**مؤجَّل عمداً (تحسينات 🟡 خارج نطاق هذا الـ PR):** استضافة قوام الكرة محلياً،
Pydantic response models الكاملة، وترحيل تلميحات النوع القديمة (`Optional`→`|`).

---

*تقرير مُولَّد آلياً بمراجعة كاملة للمستودع بتاريخ 2026-07-30. كل ملاحظة مرتبطة بموضع محدد (file:line) قابل للتحقق.*
