# تبعيات التشغيل فقط. أدوات التطوير في requirements-dev.txt
# (كان الاثنان مدموجين، فتشحن صورة الإنتاج pytest و ruff بلا داعٍ).
#
# ملاحظة: fastapi مثبّت على إصدار يسمح بـ starlette >= 1.x — التثبيت السابق
# (fastapi 0.115) كان يقفل starlette على 0.38.6 التي تحمل 7 ثغرات معروفة.
# starlette مُثبَّت صراحةً كي لا يعود القفل عبر حلّ التبعيات.
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
aiosqlite==0.20.0
httpx==0.28.1
apscheduler==3.10.4
feedparser==6.0.13
pydantic==2.13.4
# يجلب python-dotenv كتبعية — لا نثبّته صراحةً
pydantic-settings==2.14.2
