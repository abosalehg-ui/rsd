# أدوات التطوير والاختبار — لا تُثبَّت في صورة الإنتاج.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.6.8
pip-audit==2.7.3
